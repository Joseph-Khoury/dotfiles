# WSL-specific PATH/bootstrap

# Keep generated Zsh files, plugin folders, and caches inside /home, not /mnt/c.
# Add WSL-specific PATH entries here only if needed.

export FASTFETCH_FULL_MIN_COLS=80
