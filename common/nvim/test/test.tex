\documentclass{article}
\begin{document}
Hello, Word!

$\omega$ 

\begin{quote}
    Using VimTeX is lots of fun! % Use `cse` to change the surrounding env, and `dse` to delete it.
    \textbf{And it's not too difficult either!} % Use `dsc` to delete the surrounding command.
\end{quote}

\begin{equation}
    \left(X + Y \right) % Use `dsd` to delete the surrounding delimiters and `csd` to change them.
\end{equation}

$ 1 + 1 = 2 $ %Use `cs$ equation` to change this into an equation block.

\end{document}
